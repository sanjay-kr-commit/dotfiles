[*] custom order plugin loading
[*] custom order based on priority (automatic loading based on priority)
[*] add dynamic flag loading
[] add module for backing up config which symlinks
[] add crypfs module for backing up sensitive information
[] dependency advertisement and resolving
[] Add flag conflict management: Ensure modules negotiate and resolve flag usage conflicts.
[] Add resource conflict handling: Prevent multiple modules from modifying the same resource at once.
[] Implement priority resolution: Allow modules to decide which takes precedence when conflicts arise.
[] Add module disable/enable conflict checks: Resolve conflicts when a module is enabled or disabled, not at runtime.
[] Add dynamic flag registration checks: Prevent modules from registering the same flag twice.
[] Add package manager abstraction: Let modules check and install dependencies across different package managers.
[] Add interactive add-on configuration: Allow users to dynamically configure and enable oh-my-zsh add-ons.
[] Add private git repo for sensitive configs: Track encrypted changes inside the private directory.
[] Integrate cryptfs module: Mount private data if a key is present, else hide it.
[] Add Dockerfile for environment replication: Provide a portable way to spin up the environment anywhere.
[] Add plugin for backing up explicit package install for restoration
[] Move the dependency resolution to enable module function, so a priority load can be made
[] Rewrite of whole runtime for static loading and lazy loading
[] Rewrite ./module/fzfForPreviousCmd for better handling history
