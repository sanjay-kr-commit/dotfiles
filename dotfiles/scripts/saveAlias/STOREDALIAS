alias push='gitPass push sanjay'
alias hi='echo hi'
alias removeAndroidStudioLock='rm .var/app/com.google.AndroidStudio/config/Google/AndroidStudio2024.1/.lock'
alias xclip='xclip -selection c'
alias :q='exit'
alias tmux='/usr/bin/tmux -u'
alias :s='poweroff'
alias :r='reboot'
alias dotpush='currentDir=$(pwd) && cd $HOME/dotfiles && push && cd $currentDir && unset currentDir'
alias gitRemove='git rm --cached'
alias c+='zig c++'
alias updateList='apt list --upgradable| less'
alias ratemirror='rate-mirrors arch | sudo tee /etc/pacman.d/mirrorlist && rate-mirrors endeavouros | sudo tee /etc/pacman.d/endeavouros-mirrorlist'
alias ratemirrorandupdate='rate-mirrors arch | sudo tee /etc/pacman.d/mirrorlist && rate-mirrors endeavouros | sudo tee /etc/pacman.d/endeavouros-mirrorlist && eos-update --aur'
alias :i='nvim .'
alias removeOrphanPackages='sudo pacman -Rns $(pacman -Qdtq)'
alias removePackageCache='paccache -r && paccache -ruk0'
alias removeOldJournalEntries='journalctl --vacuum-time=4weeks'
alias updateMirror='sudo reflector --protocol https --verbose --latest 25 --sort rate --save /etc/pacman.d/mirrorlist && eos-rankmirrors --verbose && eos-update --aur'
alias :si='sudo nvim .'
alias pull='git pull'
alias :c='clear'
alias :p='git pull'
alias c='zig cc -Wmain-return-type'
alias pythonPackageRebuild='yay -S $(yay -Qoq /usr/lib/"$(readlink /usr/bin/"$(readlink /usr/bin/python)" )") --answerclean All --noconfirm'
alias packageSearch='yay -F'
alias updatePackageContentDatabase='yay -Fy'
alias qdbusviewer='/usr/lib/qt6/bin/qdbusviewer'
alias kill-session='tmux kill-session -t'
alias signer='/home/sanjay/build-tools/35.0.1/apksigner'
alias toggleNightColor='/usr/bin/qdbus6 org.kde.kglobalaccel /component/kwin invokeShortcut "Toggle Night Color"'
alias open='xdg-open'
alias btop='btop --force-utf'
alias :l='nvim leetcode.nvim'
alias :v='nvim'
alias :sv='sudoedit'
alias send='kdeconnect-cli -d $(kdeconnect-cli -a --id-only) --share'
alias downmp3='yt-dlp --audio-format mp3 -x'
alias aiLab='if [[ "$(pwd)" != "$HOME/college/ai" ]] ; then ; cd ~/college/ai ; fi && if [[ "$(which deactivate)" == "deactivate not found" ]] ; then ; activateVenv; fi && python $(which idle) $(date +%Y-%m-%d).py && deactivate'
alias lastCommand=" tail -n1 ~/.zsh_history | grep -o \";.*\" | sed -e 's/;//g'"
alias update-initramfs='sudo dracut --force --hostonly --no-hostonly-cmdline /boot/initramfs-linux.img $(uname -r)'
alias today='nvim ~/Documents/Obsidian/Daily\ Logs/$(date +%Y-%m-%d).md'
alias :t='today'
alias :o='nvim ~/Documents/Obsidian/Homepage.md'
alias lsm='ls -la | awk  "{ printf \$0 ; printf \" \" ; printf (\$5/1024)/1024 ; print \"\" }"'
alias searchwiki='echo -n "query: " && read -r query && query=$(echo $query | jq -sRr @uri | sed -e "s/%0A//g") && echo searching $query && redirectedPage=$(curl -Ls -o /dev/shm/content -w "%{url_effective}\n" https://wiki.archlinux.org/index.php\?limit\=1000000\&search\=$query) && if [[ $redirectedPage =~ ".*/title/.*" ]] ; then cat /dev/shm/content | html2text | nvim ; else cat /dev/shm/content | html2text | grep -o "\* *\[.*\] *(.*)" | grep -o "(.*)" | sed -e "s#(/#https://wiki.archlinux.org/#" -e "s/[()]//g" | grep -o "https[^ ]*" | fzf | xargs -I {} curl {} 2>/dev/null | html2text >/dev/shm/content && nvim /dev/shm/content ; fi && unset pageName && unset query'
